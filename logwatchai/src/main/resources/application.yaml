spring:
  application:
    name: logwatchai

  main:
    allow-circular-references: true

  datasource:
    hikari:
      initialization-fail-timeout: 60000
      connection-timeout: 30000
    url: jdbc:mariadb://db:3306/logwatchai
    username: loguser
    password: logpass
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update # Schema automatisch anpassen (für Dev/PoC)
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  mail:
    host: smtp.example.com
    port: 587
    username: your-email@example.com
    password: your-email-password
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true

server:
  port: 8080

logging:
  level:
    root: INFO
    #com.bbu.solution: DEBUG
    #org.hibernate.SQL: debug
    #org.hibernate.type.descriptor.sql: trace
    #com.theokanning.openai: DEBUG
    #org.springframework.web.client.RestTemplate: DEBUG
    #org.springframework: DEBUG
    #io.jsonwebtoken: DEBUG

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 3600000 # 1 Stunde

ai:
  api-key: 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
  model: gpt-4o-mini
  enabled: true # kann bei Bedarf deaktiviert werden

app:
  config-file: /app/config/logwatchai.yaml
  # hier kann später zusätzliche App-Konfiguration stehen